import 'package:flutter/painting.dart';

class AppColors {
  AppColors._();

  // Neutrals — warm stone ramp
  static const ink = Color(0xFF1C1917);
  static const ink2 = Color(0xFF57534E);
  static const ink3 = Color(0xFFA8A29E);
  static const ink4 = Color(0xFFD6D3D1);
  static const bg = Color(0xFFFAFAF8);
  static const surface = Color(0xFFFFFFFF);
  static const field = Color(0xFFF5F5F4);
  static const line = Color(0xFFE7E5E4);
  static const line2 = Color(0xFFF2F0EF);

  // Primary accent — warm orange
  static const accent = Color(0xFFF97316);
  static const accentDeep = Color(0xFFC2570A);
  static const accent50 = Color(0xFFFFF7ED);
  static const accent200 = Color(0xFFFED7AA);

  // Secondary accent — warm teal (dietary / cuisine / info)
  static const teal = Color(0xFF0D9488);
  static const tealDeep = Color(0xFF0F766E);
  static const teal50 = Color(0xFFF0FDFA);
  static const teal200 = Color(0xFF99F6E4);

  // Semantic states
  static const ok = Color(0xFF16A34A);
  static const okBg = Color(0xFFDCFCE7);
  static const warn = Color(0xFFD97706);
  static const warnBg = Color(0xFFFEF3C7);
  static const danger = Color(0xFFDC2626);
  static const dangerBg = Color(0xFFFEE2E2);
}
